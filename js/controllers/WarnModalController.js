import BaseController from './BaseController.js';

// TODO: - THIS CONTROLLER IS NOT USED YET- Warn the user when entering in item-detail in case is not logged in
export default class WarnModalController extends BaseController{
  constructor(element){
    super(element);
  }
  
}